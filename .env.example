# Copy this file to .env and fill in the values from your Supabase project
# (Project Settings -> API). Without them the app simply stays local-only.
#
# Only EXPO_PUBLIC_ variables reach the app. Never put the service_role key here.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
